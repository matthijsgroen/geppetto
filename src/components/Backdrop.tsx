import styled from "styled-components";

const Backdrop = styled.div`
  top: 0;
  bottom: 0;
  left: 0;
  right: 0;
  position: absolute;
  background: rgba(0.2, 0.2, 0.2, 40%);
`;

export default Backdrop;
